services:
  yetiapp:
    image: node:20-alpine
    container_name: yetiapp
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    command: sh -c "npm install && npm run dev"
    environment:
      NODE_ENV: development
